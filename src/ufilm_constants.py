max_rects_per_class = 4
logging_dest_dir = "logs"
# note: this latency can be different if debug mode is on (more writing)
# Main purpose of this timeout is to finish an issue.
issue_close_latency_secs = 25.0
